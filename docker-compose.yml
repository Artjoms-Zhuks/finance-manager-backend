version: "3.8"
services:
  db:
    image: postgres:16
    container_name: finance_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: finance_manager
    ports:
      - "5432:5432"
